cmake_minimum_required(VERSION 2.8)
project( python )
find_package( OpenCV )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( python circdetect.cpp )
target_link_libraries( python ${OpenCV_LIBS} )
